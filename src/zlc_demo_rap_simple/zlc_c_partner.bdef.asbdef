projection;
strict ( 2 );

define behavior for ZLC_C_PARTNER alias Partner
{
  use create;
  use update;
  use delete;

  // Actions
  use action fillEmptyStreets;
  use action clearAllEmptyStreets;
  use action copyLine;
  use action withPopup;

}